# PRD — RUSTGAME: Rust-like Open-World Survival

| Field | Value |
|---|---|
| Product | RUSTGAME — بقاء عالم مفتوح بأسلوب Rust (هوية مستقلة) |
| Platforms | Web (Three.js) + Android (Capacitor) + Godot 4.x (نسخة موازية) |
| Repo | https://github.com/MOT1209/rustgame |
| Version | 1.1.0 (build 15) — Phase 1 مكتملة |
| Owner | MOT1209 |
| Status | Phase 1 ✅ Done · Phase 2 📋 Planned |
| No-copy rule | ممنوع نسخ أصول/محتوى/هوية Rust (Facepunch). كل الأصول CC0 أو مولّدة. |

---

## 1. Vision

> "أنا في عالم مفتوح خطير، أبدأ بموارد قليلة، أجمع الموارد، أحافظ على صحتي ومائي وطعامي، أصنع الأدوات، أبني مأوى، أستكشف العالم، وأنجو من الليل."

تجربة بقاء متكاملة تعمل على المتصفح والموبايل، بأداء مقبول وأعطال صفرية.

## 2. Users

| User | Needs |
|---|---|
| لاعب ديسكتوب | تحكم كيبورد/ماوس، جلسات طويلة، بناء متقدم |
| لاعب موبايل (APK/PWA) | أزرار لمس، أداء خفيف، حفظ تلقائي موثوق |
| مطوّر/مساهم | وحدات نظيفة + اختبارات آلية + حفظ مُصدَر |

## 3. Goals / Non-Goals

**Goals (Phase 2):**
1. قتال حيواني ذو معنى (ذئب/دب/عالِم) مع نهب متوازن.
2. طبخ متقدم + تلف طعام (نموذج البيانات جاهز: `spoilTime`).
3. فرن صهر (تحويل خام → fragments).
4. خريطة مصغرة + بوصلة + مناطق POI.
5. تحسين موبايل: أزرار سياقية E، أداء.

**Non-Goals (مؤجلة صراحة):** أسلحة نارية متقدمة، PvP/ملتي بلاير، مركبات، كهرباء، غارات (Raiding)، عشائر/سيرفرات، مهام، هليكوبتر.

## 4. Functional Requirements

### 4.1 Survival (موجود — Phase 1)
- [x] صحة/جوع/عطش/ستامينا/حرارة/إشعاع/نزيف — كلها من `SURVIVAL_CONFIG`.
- [x] حالات الجوع/العطش الأربع + ضرر التجويع/الجفاف (غير فوري).
- [x] ستامينا: ركض/قفز/جمع، منع الركض تحت الحد.
- [x] حرارة: ليل/مطر/نار مخيم/ماء؛ تجميد → إبطاء + ضرر حرج.
- [x] `DamageSystem.applyDamage` نقطة مركزية (7 أنواع)؛ شفاء محدود + تجدد طبيعي.

### 4.2 Food & Water (موجود — Phase 1)
- [x] توت/لحم نيء (ضار)/مطبوخ/معلبات/ماء/ضمادات + استخدام بالنقر المزدوج.
- [x] بحيرة (E للشرب) + حاوية مخيم محدودة.
- [ ] Phase 2: تلف الطعام + وصفات طهي + تنقية ماء.

### 4.3 Gathering & Nodes (موجود — Phase 1)
- [x] شجرة/صخر/حديد/كبريت/قنب/توت/برميل/صناديق — صحة/عائد/أداة/زمن respawn من الإعدادات.
- [x] مكافأة الأداة الصحيحة (×2) + تكلفة ستامينا.
- [ ] Phase 2: عقد معدن عالي في مناطق خطرة.

### 4.4 Crafting (موجود — Phase 1)
- [x] فأس حجري/شعلة/نار/ضمادة/صندوق + تحقق يمنع السالب.
- [ ] Phase 2: فرن + قائمة صهر + طابور صياغة زمني (`craftTime`).

### 4.5 Inventory & Storage (موجود — Phase 1)
- [x] `StorageInventory` مستقل: stack/add/remove/consume/transfer + تسلسل.
- [x] صندوق المخيم + صناديق قابلة للوضع (12 stack) + حفظ المحتويات.
- [ ] Phase 2: فرز/نقل سريع، خزائن مقفلة.

### 4.6 Building (موجود — يُحفظ)
- [x] 5 tiers + TC + أبواب + E للإصلاح/الترقية — **لا يُعاد بناؤه**.
- [ ] Phase 2: تحلل (Decay) للمباني المهجورة فقط.

### 4.7 World: Day/Night + Weather (موجود — Phase 1)
- [x] يوم 800s + عدّاد أيام + ساعة HUD + طقس (صافٍ/غائم/مطر) يؤثر على الضوء/الضباب/الحرارة.
- [ ] Phase 2: عاصفة + برق + تأثير على الحيوانات.

### 4.8 Interaction (موجود — Phase 1)
- [x] E موحدة: جمع/تخزين/شرب/نهب/استخدام/باب — capture-phase لا يكسر E الحالية.
- [ ] Phase 2: زر E سياقي على الموبايل.

### 4.9 Save/Load (موجود — Phase 1)
- [x] حفظ v2: لاعب/مخزون/صناديق/respawn/طقس/يوم/مباني/وقت + ترحيل v1 + حماية تلف + autosave (60s + إخفاء الصفحة + أحداث كبرى).
- [ ] Phase 2: فتحات حفظ متعددة + تصدير/استيراد JSON.

### 4.10 Combat & AI (Phase 2 — جديد)
- [ ] هجمات حيوانية بأنماط (مطاردة/انسحاب/قطيع ذئاب بسيط).
- [ ] ضرر المشاجرة + رماح/قوس توازن أولي (بدون أسلحة نارية).
- [ ] نهب الجثث بـ E.
- معيار القبول: مواجهة ذئب تُربح بالأدوات الحجرية مع استخدام ضمادة واحدة.

## 5. Technical Requirements
- Web: ES modules خالصة للمنطق (تُختبر بـ `node tests/phase1.test.mjs` — حالياً 30/30).
- Godot (`rust-1/`): نسخة موازية — أي تغيير ويب لا يكسرها.
- Performance: لا DOM writes كل frame إلا عند التغير؛ لا `new Vector3` في الحلقة؛ المطر بلا جزيئات؛ الصور < 200KB.
- Compat: Web + Capacitor APK + PWA offline (`sw.js`)؛ لا اعتماد على كيبورد فقط.
- Config: أي رقم gameplay جديد يدخل `SURVIVAL_CONFIG` أولاً.

## 6. QA Plan (TestSprite)
- بوابة الاختبار: `index.html` عبر سيرفر محلي (مثال: `http://localhost:8901`).
- `TestSprite` MCP مهيأ في `~/.config/opencode/opencode.jsonc` (أعد تشغيل opencode لتفعيله).
- سيناريوهات الدخان: إقلاع بلا أخطاء كونسول → بدء اللعب → جمع → أكل/شرب → صياغة → بناء → تخزين → حفظ/تحميل → موت/إحياء.
- بوابة القبول: صفر أخطاء كونسول + كل السيناريوهات خضراء + `node tests/phase1.test.mjs` أخضر.

## 7. Milestones (Phase 2)

| # | Deliverable | Acceptance |
|---|---|---|
| 2.1 | قتال حيواني + نهب E | مواجهة ذئب قابلة للربح |
| 2.2 | فرن + صهر + تلف طعام | خام → fragments؛ لحم يفسد بعد N |
| 2.3 | خريطة/بوصلة + POI | تحديد المعالم والبحيرة |
| 2.4 | موبايل: زر E سياقي + أداء | اللعب باللمس كامل الحلقة |
| 2.5 | QA شامل + إصدار 1.2.0 | TestSprite أخضر + buildNumber جديد |

## 8. Risks
| Risk | Mitigation |
|---|---|
| `game.js` أحادي ضخم (2600+ سطر) | استخراج تدريجي لوحدات؛ لا نقل عشوائي |
| مكتبة `unified-game-controls.js` خارج المستودع | طبقة `js/input/` تجريدية فوقها |
| CDN (three.js/font-awesome) أوفلاين | PWA cache في `sw.js` + فحص عند الإقلاع |
| مفاتيح API في الإعداد | تدوير مفتاح TestSprite عند التسريب؛ `{env:...}` للأسرار |

## 9. Open Questions
1. هل نضيف فتحات حفظ متعددة في 2.x أم لاحقاً؟
2. هل الذئاب تسقط لحم نيء مباشرة أم عبر ذبح E؟ (المقترح: E)
3. حد stack الخشب 1000 — هل يُخفض لزيادة التحدي؟

---
*آخر تحديث: 2026-09-24 · يتوافق مع `version.json` build 15.*
